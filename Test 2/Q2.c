#include<stdio.h>
int main(){
    int a,b;
        printf("Enter two numbers:");
        scanf("%d %d",&a,&b);
    
    (a>b)? printf("Number a is larger=%d"):printf(" Number b is larger=%d");

    return 0;
}